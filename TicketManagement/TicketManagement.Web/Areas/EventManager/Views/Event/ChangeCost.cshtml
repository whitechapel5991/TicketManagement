@model TicketManagement.Web.Areas.EventManager.Data.AreaPriceViewModel
@{
    Layout = null;
}

<div class="modal-content">
    <div class="modal-header">
        <h5 class="modal-title">@TicketManagementResource.ChangeAreaPriceDisplay</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
            
    @using (Ajax.BeginForm("ChangeCost", new { area = "EventManager", controller = "Event" }, 
        new AjaxOptions { HttpMethod = WebRequestMethods.Http.Post }, 
        new { id = "form-changeCost" }))
    {
        <div class="modal-body">
            <h4>@TicketManagementResource.SetAreasCosts</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger", id = "errorMsg" })
            @Html.HiddenFor(model => model.EventAreaId)
            <div class="form-group">
                @Html.LabelFor(model => model.Price, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Price, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Price, "", new { @class = "text-danger" })
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="submit" class="btn btn-primary">@TicketManagementResource.Save</button>
            <button type="button" class="btn btn-secondary" data-dismiss="modal">@TicketManagementResource.Back</button>
        </div>
    }
</div>

<script type="text/javascript">
    changeCostPopupPost('#changeAreaCostModDialog', '#form-changeCost');
</script>